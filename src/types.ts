export interface ApplicationDoc {
    role: string
    start_date: string;
    stage: string;
    img: string;
    source: string;
    company_name: string;
    status: string;
    rejected: boolean;
    history: History
}